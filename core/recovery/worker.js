"use strict";
const recovery=require("./index");
async function processJob(id,executor){const job=recovery.startJob(id),started=Date.now();try{const result=await executor(job);const action=recovery.recordAction(id,{type:"sandbox_verification",result:{ok:Boolean(result?.ok),code:result?.code??null,timedOut:Boolean(result?.timedOut)}});const verification={patchApplied:result?.patchApplied===true,sandboxPassed:result?.sandboxPassed===true,testsPassed:result?.testsPassed===true,ciPassed:result?.ciPassed===true,regressionDetected:result?.regressionDetected===true,filesChanged:Array.isArray(result?.filesChanged)?result.filesChanged:[],runtimeSeconds:Math.ceil((Date.now()-started)/1000)};if(verification.runtimeSeconds > job.budget.maxRuntimeSeconds) return recovery.updateJob(id,{status:"frozen",lastDecision:{decision:"frozen",reason:"recovery_budget_exceeded"}}); return {job:recovery.gate(id,verification),action,verification}}catch(error){return recovery.updateJob(id,{status:"retryable",lastError:String(error?.message||error)})}}
module.exports={processJob};
